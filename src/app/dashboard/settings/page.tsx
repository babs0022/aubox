export default function SettingsPage() {
  return (
    <div>
      <p className="dash-kicker">Account</p>
      <h1 className="mt-2 text-3xl font-bold text-[var(--ink)]">Settings</h1>
      <div className="dash-frame mt-6 p-6">
        <p className="text-sm text-[var(--muted)]">Workspace preference settings will appear here as we ship role controls and custom alerts.</p>
      </div>
    </div>
  );
}
